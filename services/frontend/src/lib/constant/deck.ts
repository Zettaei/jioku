import type { DeckExtraSetting } from "$lib/types/deck"


export const deckDefaultHeader = [
    "Kanji",
    "Reading",
    "Meaning"
]


export const deckDefaultSetting: DeckExtraSetting = {
    newLimit: 5,
    dueLimit: 5
}